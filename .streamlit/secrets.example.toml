# Copy only the names to Streamlit Secrets and replace all dummy values there.
# Never commit the real values.
SUPABASE_URL = "https://your-project.supabase.co"
SUPABASE_SECRET_KEY = "sb_secret_REPLACE_ME"
DECISION_LOG_PASSWORD = "replace-with-a-long-unique-password"
